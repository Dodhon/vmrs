How to update “VMRS Chatbot - Technical.pptx/PDF” (step by step, easy mode)

Start
1) Make a copy of the deck so the original stays safe.
2) Open the copy in PowerPoint/Keynote.
3) Use slide sorter view so you can jump around.

Update each slide
1. Title: leave as-is. Optional: add a small subtitle “Technical appendix to business deck.”
2. Context: keep the words. Add one short line: “Goal: map vendors to VMRS so parts match and the chatbot can answer.”
3. Data look: replace the big screenshot with three bullet points:
   - “VMRS master: 66,729 rows (289 systems, 3,471 assemblies, 35,724 components).”
   - “Vendor cleaned: 20,348 parts (~373 have no VMRS code).”
   - “Source: build-guide/validation snapshot.”
4. Demo: under the two questions, add tiny notes: “grille → works” and “flux capacitor → nice ‘not found’ reply.”
5. Savings & Cost: keep numbers. Add one tiny line: “Data cleanup still leaves typos/empties, so we validate later.”
6–7. Previous initiative charts: keep. Add a small caption on one corner: “$160k savings on 240 parts → bigger upside on 19k parts.”
8. Problem vs Solution: keep boxes. Add one short line under the right side: “We map vendors → VMRS (System–Assembly–Component) so parts compare fairly.”
9. The Approach: swap the little graph for a simple pipeline picture with three parts:
   - Left lane: “Markdown → extract_vmrs_to_csv.py → combine_vmrs_csv.py → validate_vmrs_csv.py (CSV ground truth).”
   - Right lane: “matching_context.md → text_chunker → triple_extractor (temp=0) → graph_builder (checkpoints).”
   - Merge both into “merge_csv_with_json.py (CSV wins, LLM adds extras)” → Neo4j. Add a small vendor branch: “create_vendor_nodes.py + refactor_vendor_schema.py + fix_orphaned_vendor_parts.py.”
   - Slide title text: “Two ingestion paths converge into Neo4j; CSV is ground truth.”
10. Results & Limitations: keep bullets. Add: “Tests: 50 acceptance Qs + 224 stress questions.” Keep the feedback note for components. Keep “pricing needs Snowflake.”
11. Tech Stack: change text to a simple schema callout: “System ← Assembly ← Component; VendorPart → Component (MAPS_TO); Vendor → VendorPart (MANUFACTURES).” Add tiny counts: “227 systems; 2,035 assemblies; 35,974 components; 20,339 vendor parts; 517 vendors.” Note “Source: build-guide snapshot.”
12. Next Steps: add two bullets: “Capture user feedback on component picks” and “Add regression tests/metrics before releases.” Keep Snowflake access line.
13. Appendix (blank): turn this into “Backup index” with a list of backup slides (see below).
14. Savings Details: keep; add tiny note: “Cleanup service not 100% accurate; we still validate.”
15. Appendix 2: replace the 3 steps with a tiny “commands” box:
    - “python3 -m venv venv && source venv/bin/activate && pip3 install -r requirements.txt”
    - “python3 scripts/data_processing/extract_vmrs_to_csv.py”
    - “python3 scripts/data_processing/combine_vmrs_csv.py”
    - “python3 scripts/data_processing/validate_vmrs_csv.py”
    - “PYTHONPATH=. python3 scripts/import_csv_to_neo4j.py”
    - “python3 scripts/create_vendor_nodes.py”
    - “python3 scripts/refactor_vendor_schema.py”
    - “python3 scripts/fix_orphaned_vendor_parts.py”
    - “python3 tests/neo4j_acceptance/process_questions.py”
16. Appendix 3: replace the link with three bullets:
    - “Stress tests: 224 edge cases in tests/stress_test (PASS/FAIL/PARTIAL).”
    - “Merge strategy: merge_csv_with_json.py (CSV first, LLM enrich).”
    - “Failure modes: OCR typos, empty descriptions, vendor-only codes (101 components created), 373 unclassified exported.”

Add these new backup slides (after slide 16)
17. Canonical hierarchy: show System → Assembly → Component; code formats (XXX / XXX-XXX / XXX-XXX-XXX); “no orphaned codes.”
18. Tabular ETL: bullets for extract/combine/validate; mention “validation_report.json” and “validation_summary.json.”
19. LLM path: bullets for text_chunker (2k tokens, 200 overlap), triple_extractor (temp=0, strict JSON), graph_builder (checkpoints).
20. Vendor linking: MAPS_TO + MANUFACTURES; vendor code reorder; counts: 101 components created from vendor data; 721 orphans linked; 373 unclassified exported.
21. Evaluation: “50 acceptance Qs” + “224 stress Qs (9 categories)” + what PASS/FAIL/PARTIAL means.

Finishing
1) Export to PDF to check fonts and images.
2) Make sure counts are the same everywhere.
3) Save the final file as “VMRS Chatbot - Technical v2.pdf”.

Copy/paste text for each slide (ready to drop in)
- Title: “VMRS Chatbot – Technical Appendix v2 (Dec 2025)”
- Context: “Goal: map vendor parts to VMRS so parts compare fairly and the chatbot can answer.” (keep other business bullets)
- Data Inputs:
  - “VMRS master: 66,729 rows (289 sys, 3,471 asm, 35,724 comp).”
  - “Vendor cleaned: 20,348 parts (~373 unclassified).”
  - “Source: build-guide/validation snapshot.”
- Demo notes: “grille → works”; “flux capacitor → graceful not-found.”
- Savings & Cost extra line: “Data cleanup still leaves typos/empties, so we validate later.”
- Caption for previous initiative charts: “$160k savings on 240 parts → bigger upside on 19k parts.”
- Problem vs Solution add-on: “We map vendors → VMRS (System–Assembly–Component) so parts compare fairly.”
- Pipeline slide (title: “Two ingestion paths converge into Neo4j; CSV is ground truth”):
  - “Tabular: md → extract_vmrs_to_csv → combine_vmrs_csv → validate_vmrs_csv.”
  - “LLM: matching_context.md → text_chunker → triple_extractor (temp=0) → graph_builder (checkpoints).”
  - “Merge: merge_csv_with_json (CSV precedence) → Neo4j.”
  - “Vendor: create_vendor_nodes + refactor_vendor_schema + fix_orphaned_vendor_parts.”
- Results & Limits:
  - “System/Assembly confident; Component needs feedback loop.”
  - “Pricing missing until Snowflake access.”
  - “Tests: 50 acceptance Qs + 224 stress cases.”
- Tech Stack / Schema:
  - “System ← Assembly ← Component”
  - “VendorPart → Component (MAPS_TO)”
  - “Vendor → VendorPart (MANUFACTURES)”
  - “Counts: 227 sys; 2,035 asm; 35,974 comp; 20,339 vendor parts; 517 vendors (build-guide).”
- Next Steps:
  - “Capture user feedback on component picks.”
  - “Add regression metrics/gates before releases.”
  - “Get Snowflake read access (pricing).”
- Appendix 3 replacement:
  - “Stress tests: 224 edge cases (tests/stress_test) PASS/FAIL/PARTIAL.”
  - “Merge strategy: merge_csv_with_json (CSV first, LLM enrich).”
  - “Failure modes: OCR typos, empty descriptions, vendor-only codes (101 comps created), 373 unclassified exported.”
- Backup slides:
  - Canonical hierarchy: “System → Assembly → Component; Codes: XXX / XXX-XXX / XXX-XXX-XXX; Invariant: no orphaned codes.”
  - Tabular ETL: “extract_vmrs_to_csv; combine_vmrs_csv; validate_vmrs_csv → validation_report.json / validation_summary.json.”
  - LLM path: “text_chunker (2k tokens, 200 overlap); triple_extractor (temp=0, strict JSON, salvage); graph_builder (checkpoints, dedup).”
  - Vendor linking: “MAPS_TO/MANUFACTURES; reorder vendor codes; 101 components created from vendor data; 721 orphan parts linked; 373 unclassified exported.”
  - Evaluation: “50 acceptance questions; 224 stress questions across 9 categories; PASS/FAIL/PARTIAL meaning.”
