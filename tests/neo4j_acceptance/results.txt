Neo4j Acceptance Test Results - 2025-11-24

1. Question: What is the VMRS code for a mirror actuator?
Cypher:
MATCH (c:Component {code: '002-010-084'})-[:PART_OF]->(a:Assembly)-[:PART_OF]->(s:System)
RETURN c.code AS component_code, c.name AS component_name, a.code AS assembly_code, a.name AS assembly_name, s.code AS system_code, s.name AS system_name;
Result:
component_code=002-010-084
component_name=Actuator - Mirror
assembly_code=002-010
assembly_name=Mirrors
system_code=002
system_name=Cab and Sheet Metal

2. Question: What is the VMRS code for a grille?
Cypher:
MATCH (c:Component {code: '002-001-001'})-[:PART_OF]->(a:Assembly)-[:PART_OF]->(s:System)
RETURN c.code AS component_code, c.name AS component_name, a.code AS assembly_code, a.name AS assembly_name, s.code AS system_code, s.name AS system_name;
Result:
component_code=002-001-001
component_name=Grille - Radiator
assembly_code=002-001
assembly_name=Radiator Grille
system_code=002
system_name=Cab and Sheet Metal

3. Question: What is the VMRS code for an A/C receiver dryer?
Cypher:
MATCH (c:Component {code: '001-001-065'})-[:PART_OF]->(a:Assembly)-[:PART_OF]->(s:System)
RETURN c.code AS component_code, c.name AS component_name, a.code AS assembly_code, a.name AS assembly_name, s.code AS system_code, s.name AS system_name;
Result:
component_code=001-001-065
component_name=Receiver - Dehydrator Assembly (Drier)
assembly_code=001-001
assembly_name=Air Conditioning Assembly - Complete
system_code=001
system_name=Air Conditioning, Heating, and Ventilating System

4. Question: Which assemblies span across multiple vehicle systems?
Cypher:
MATCH (a:Assembly)-[:PART_OF]->(s:System)
WITH a, collect(DISTINCT s.code) AS systems
WHERE size(systems) > 1
RETURN a.name AS assembly_name, systems;
Result:
No assemblies returned (each assembly belongs to a single system in the current Neo4j snapshot).
